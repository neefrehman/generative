// https://github.com/aaronpenne/generative_art#reach

(() => {
    setup = () => {
        createCanvas(windowWidth, windowHeight);
        background(20);
    };

    draw = () => {
        background(20);
    };
})();

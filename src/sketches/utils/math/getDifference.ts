/**
 * Gets the difference between two numbers
 *
 * @param n1 The first value
 * @param n2 The second value
 *
 * @returns The absolute difference between the two numbers
 */
export const getDifference = (n1: number, n2: number): number => Math.abs(n1 - n2);
